/**
 * @agent-runtime/channel-bridge · webhook/dispatcher.ts (stub)
 * TODO REQ-005 · 真接收外部 channel webhook · 真 normalize → daemon /chat
 */
export interface WebhookDispatchInput {
  readonly channel: string;
  readonly raw: unknown;
}
