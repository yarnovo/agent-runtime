/**
 * @agent-runtime/tools · base/bash.ts (stub)
 * TODO · 真 agent 内置 Bash tool · 走 packages/sandbox exec/run-shell
 */
export interface BashToolInput {
  readonly command: string;
  readonly timeoutMs: number;
}
