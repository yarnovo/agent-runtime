/**
 * @agent-runtime/channel-h5 · index.ts (stub)
 * TODO REQ-012 · web UI plug-in · fastify register · listen 80 · static SPA + chat SSE
 *
 * 用户访问 m.<agent>.<team>.agentaily.com → daemon 直接 hit channel-h5 plug-in 渲 web UI。
 * 未来 fold akong-hr/apps/web 的 src · 复用 aily-ui 组件库。
 */
export { channelH5Plugin } from "./server.js";
