/**
 * @agent-runtime/core · session.ts (stub)
 *
 * PI session 包装 · 拿 packages/session 出来的 session record + packages/llm provider · 真起一次对话。
 * TODO Phase 1 · 真接 PI Session 抽象
 */
export interface RuntimeSession {
  readonly id: string;
}
