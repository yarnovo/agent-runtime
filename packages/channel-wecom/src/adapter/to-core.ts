/**
 * @agent-runtime/channel-wecom · adapter/to-core.ts (stub)
 * TODO REQ-012 · 真 forward 企业微信 inbound → @agent-runtime/core 的 PI session.prompt()
 */
import type { WecomInboundMessage } from "../webhook/receive.js";

export async function forwardWecomToCore(_msg: WecomInboundMessage): Promise<void> {
  // stub
}
