/**
 * @agent-runtime/channel-wecom · api/send-message.ts (stub)
 * TODO REQ-012 · 真企业微信主动消息 API · POST /cgi-bin/message/send · 推 agent 回复
 */
export async function sendWecomMessage(_input: {
  readonly toUser: string;
  readonly agentId: string;
  readonly text: string;
}): Promise<void> {
  // stub
}
