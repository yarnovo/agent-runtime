/**
 * @agent-runtime/channel-wecom · api/access-token.ts (stub)
 * TODO REQ-012 · 真企业微信 access_token 获取 + 缓存 + 自动 refresh (7200s TTL)
 */
export async function getWecomAccessToken(): Promise<string> {
  return "";
}
