/**
 * @agent-runtime/lifecycle · evolve.ts (stub)
 * TODO REQ-008 · 真 agent 进化 · soul 升级 / skill 累积 / memory 整理
 */
export interface EvolveStep {
  readonly fromVersion: string;
  readonly toVersion: string;
}
