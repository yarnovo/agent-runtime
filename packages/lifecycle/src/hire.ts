/**
 * @agent-runtime/lifecycle · hire.ts (stub)
 * TODO REQ-008 · 真 agent 真 hire (新 agent role / 新 maintainer 接入)
 */
export interface HireOrder {
  readonly role: string;
  readonly soulPath: string;
}
