/**
 * @agent-runtime/lifecycle · snapshot.ts (stub)
 * TODO REQ-008 · 真打 agent 状态 snapshot · 落 OSS (packages/storage)
 */
export interface SnapshotMeta {
  readonly snapshotId: string;
  readonly takenAt: string;
}
