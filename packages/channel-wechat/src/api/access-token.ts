/**
 * @agent-runtime/channel-wechat · api/access-token.ts (stub)
 * TODO REQ-012 · 真 access_token 获取 + 缓存 + 自动 refresh (微信 7200s TTL)
 */
export async function getWechatAccessToken(): Promise<string> {
  return "";
}
