/**
 * @agent-runtime/channel-wechat · adapter/to-core.ts (stub)
 * TODO REQ-012 · 真 forward 微信 inbound → @agent-runtime/core 的 PI session.prompt()
 */
import type { WechatInboundMessage } from "../webhook/receive.js";

export async function forwardWechatToCore(_msg: WechatInboundMessage): Promise<void> {
  // stub
}
