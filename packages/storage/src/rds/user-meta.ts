/**
 * @agent-runtime/storage · rds/user-meta.ts (stub)
 * TODO · 真 RDS 落 user metadata (preferences / quota_used / agent registry)
 */
export interface UserMetaRow {
  readonly userId: string;
  readonly displayName: string;
}
