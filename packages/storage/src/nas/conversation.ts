/**
 * @agent-runtime/storage · nas/conversation.ts (stub)
 * TODO REQ-002 · 真 NAS jsonl 落 conversation
 */
export interface ConversationRecord {
  readonly sessionId: string;
  readonly path: string;
}
