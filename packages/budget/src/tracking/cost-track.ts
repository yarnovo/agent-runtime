/**
 * @agent-runtime/budget · tracking/cost-track.ts (stub)
 * TODO · 真按 provider 单价 · 真累计 token cost
 */
export interface CostRecord {
  readonly inputTokens: number;
  readonly outputTokens: number;
  readonly costUsd: number;
}
