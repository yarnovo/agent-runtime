/**
 * @agent-runtime/sandbox · adapters/interface.ts (stub)
 * TODO REQ-006 · 真 sandbox provider 公共抽象 (boot / exec / fs / destroy)
 */
export interface SandboxAdapter {
  readonly id: string;
  readonly displayName: string;
}
