/**
 * @agent-runtime/sandbox · pool/manager.ts (stub)
 * TODO REQ-006 · 真 warm pool · pre-boot N · LRU return · health check
 */
export interface SandboxPoolManagerConfig {
  readonly preBootSize: number;
  readonly maxSize: number;
}
