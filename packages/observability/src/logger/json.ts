/**
 * @agent-runtime/observability · logger/json.ts (stub)
 * TODO · 真 JSON line logger · 出 stdout · 给 ECS / FC log collector
 */
export interface JsonLogger {
  readonly level: "debug" | "info" | "warn" | "error";
}
