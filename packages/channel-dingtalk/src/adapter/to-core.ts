/**
 * @agent-runtime/channel-dingtalk · adapter/to-core.ts (stub)
 * TODO REQ-012 · 真 forward 钉钉 inbound → @agent-runtime/core 的 PI session.prompt()
 */
import type { DingtalkInboundMessage } from "../webhook/receive.js";

export async function forwardDingtalkToCore(_msg: DingtalkInboundMessage): Promise<void> {
  // stub
}
