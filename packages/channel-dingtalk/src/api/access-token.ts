/**
 * @agent-runtime/channel-dingtalk · api/access-token.ts (stub)
 * TODO REQ-012 · 真钉钉 access_token 获取 + 缓存 + 自动 refresh (7200s TTL)
 */
export async function getDingtalkAccessToken(): Promise<string> {
  return "";
}
