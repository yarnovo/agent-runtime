/**
 * @agent-runtime/llm · providers/interface.ts (stub)
 * TODO · 真 LLM provider 公共抽象 (chat / streamChat / countTokens)
 */
export interface LLMProvider {
  readonly id: string;
  readonly displayName: string;
}
