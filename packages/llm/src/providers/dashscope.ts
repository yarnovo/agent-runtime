/**
 * @agent-runtime/llm · providers/dashscope.ts (stub)
 * TODO · 真把 apps/daemon/src/llm.ts 真切到这里
 */
export interface DashScopeProviderConfig {
  readonly baseUrl: string;
  readonly defaultModel: string;
}
