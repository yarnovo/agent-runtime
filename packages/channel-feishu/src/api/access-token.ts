/**
 * @agent-runtime/channel-feishu · api/access-token.ts (stub)
 * TODO REQ-012 · 真 tenant_access_token 获取 + 缓存 + 自动 refresh (lark 2h TTL)
 */
export async function getFeishuTenantAccessToken(): Promise<string> {
  return "";
}
