/**
 * @agent-runtime/channel-feishu · adapter/to-core.ts (stub)
 * TODO REQ-012 · 真 forward 飞书 inbound → @agent-runtime/core 的 PI session.prompt()
 */
import type { FeishuInboundMessage } from "../webhook/receive.js";

export async function forwardFeishuToCore(_msg: FeishuInboundMessage): Promise<void> {
  // stub
}
