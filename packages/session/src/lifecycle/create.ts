/**
 * @agent-runtime/session · lifecycle/create.ts (stub)
 * TODO · 真新建 session · 分配 sessionId + 初始化 messages
 */
export interface CreateSessionInput {
  readonly userId: string;
  readonly agentId: string;
}
