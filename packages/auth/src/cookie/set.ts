/**
 * @agent-runtime/auth · cookie/set.ts (stub)
 * TODO · 真签名 + Set-Cookie · HttpOnly / Secure / SameSite
 */
export interface CookieSetOptions {
  readonly maxAge: number;
  readonly secure: boolean;
}
