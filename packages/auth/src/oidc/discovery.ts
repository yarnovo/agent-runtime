/**
 * @agent-runtime/auth · oidc/discovery.ts (stub)
 * TODO · 真拉 .well-known/openid-configuration · 缓存 jwks
 */
export interface OIDCDiscoveryDoc {
  readonly issuer: string;
  readonly jwksUri: string;
}
