{
  "name": "@yarnovo/agent-runtime",
  "version": "0.2.0",
  "private": true,
  "description": "阿空 agent runtime · monorepo · 1 daemon app + 11 packages · 极致细分目录架构",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "dev": "pnpm --filter @agent-runtime/daemon dev",
    "start": "pnpm --filter @agent-runtime/daemon start"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^4.1.5"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.0.0",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yarnovo/agent-runtime.git"
  }
}
