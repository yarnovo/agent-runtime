import type { PipelineContext } from "./context.js";

/**
 * Step 04 · session-fetch · 取 / 建 session (resume vs new)。
 * TODO REQ-002 · 真接 packages/session (store / lifecycle / tree)
 */
export async function sessionFetchStep(ctx: PipelineContext): Promise<PipelineContext> {
  return ctx;
}
